# 无界 14 2026 风扇控制接口研究记录

记录日期：2026-09-07

## 目的与安全边界

本文持久化记录 `EmdAcpi_FanControl` 的静态分析结果，供未来在合适的测试设备上继续研究。

当前安全原则：

- 不在这台日常使用的笔记本上测试风扇控制。
- 不调用 `EmdAcpi_FanControl.FanControl`。
- 暂不调用名为只读、但内部仍包含 EC 写入的 `GetFanSpeed`。
- 不直接写 EC 地址，不使用 PawnIO、WinIO 或其他驱动发送 EC 写命令。
- 当前程序只保留已经验证的 `GFNS` 转速读取、`GPFM` 性能模式读取和 WMI 事件订阅。

## 目标机器

- 厂商：MECHREVO
- 机型：WUJIE Series
- 主板：WUJIE Series-Lark4-LNL
- 平台：Lunar Lake
- BIOS：EM_LNL326_V1.0.19
- BIOS 日期：2026-03-14

这些结论只适用于上述固件版本。升级 BIOS 或 EC 固件后必须重新导出并比较 ACPI 表。

## Windows WMI 类

类名：`EmdAcpi_FanControl`

GUID：`{D06385DE-BC53-4B9B-85DC-674936D9549F}`

Windows 注册的方法：

| 方法 | WMI 方法编号 | 参数 | 元数据描述 |
|---|---:|---|---|
| `GetFanSpeed` | 1 | `Data: UInt64`，输入/输出共用 | Get Fan Speed |
| `FanControl` | 2 | `Data: UInt64`，输入/输出共用 | Fan Control |

DSDT 的 `_WDG` 中，该 GUID 后面的对象编号为 ASCII `AF`，因此 Windows 的调用会进入 ACPI 方法 `WMAF`。

证据位置：

- `_WDG` 中的 GUID 和对象编号：`acpi_tables/DSDT.dsl` 第 25991 行附近
- `WMAF` 方法入口：`acpi_tables/DSDT.dsl` 第 27181 行

## `GetFanSpeed` 的实际行为

`GetFanSpeed` 对应 `WMAF` 的方法编号 1。它的固件流程为：

1. 创建 8 字节返回缓冲区。
2. 将返回状态 `RTNC` 初始化为 1。
3. 调用 `ECWT(0x10, RefOf(XXTT))`，即向 EC 字段 `XXTT` 写入 `0x10`。
4. 等待 10 毫秒并确认 `XXTT == 0x10`。
5. 读取 `TLIE`，放入返回缓冲区第 2 字节 `ECVN`。
6. 读取 `TLID`，放入返回缓冲区第 3 字节 `ECVM`。
7. 成功时令 `RTNC = 0`。

相关 EC 字段位置：

- `XXTT`：`0x30`
- `TLID`：`0x32`
- `TLIE`：`0x33`

重要结论：

- 这个方法虽然名为 `GetFanSpeed`，但内部不是严格只读；它会先写 `XXTT`。
- 它没有读取已经确认的测速字段 `F1RH/F1RL/F2RH/F2RL`。
- `TLIE/TLID` 的含义和单位尚未确认。它们可能是目标等级、控制值、状态值或经过编码的速度，不应直接解释成 RPM。
- 在没有运行时对照之前，不能用它替代已经验证的 `GFNS`。

## `FanControl` 的实际行为

`FanControl` 对应 `WMAF` 的方法编号 2，只接受固件明确处理的两个输入值：

### 输入值 1

1. 向 `ZZTT` 写入 `0x12`。
2. 确认 `ZZTT == 0x12`。
3. 向 `TIZZ` 写入 `0x00`。
4. 成功时返回状态 0。

### 输入值 2

1. 向 `ZZTT` 写入 `0x12`。
2. 确认 `ZZTT == 0x12`。
3. 向 `TIZZ` 写入 `0x01`。
4. 成功时返回状态 0。

其他输入返回错误状态 2。

目前不知道输入值 1 和 2 分别代表什么。可能是启用/关闭某种风扇功能、自动/手动模式或增压模式，但 ACPI 表本身不足以证明任何一种解释。不得依靠方法名猜测并执行。

证据位置：`acpi_tables/DSDT.dsl` 第 27210 行至 27264 行。

## 与已验证 RPM 路径的区别

当前实时监视器使用 `OemWMIMethod.OemWMIfun` 的固定请求：

- 风扇 1：`02 01 00`
- 风扇 2：`02 01 01`

该请求进入 `WMCD -> GFNS`，随后直接读取：

- 风扇 1：`F1RH/F1RL`，EC 偏移 `0x76/0x77`
- 风扇 2：`F2RH/F2RL`，EC 偏移 `0x79/0x7A`

`GFNS` 不先写 EC 命令字段，且已经得到并验证过：

- `0x0951 = 2385 RPM`
- `0x0909 = 2313 RPM`

因此，`GFNS` 仍是当前可信且严格只读的 RPM 来源。

## 未来研究前必须解决的问题

1. `TLIE` 和 `TLID` 分别代表哪把风扇，还是代表其他状态？
2. 返回值是 RPM、百分比、PWM 等级还是固件内部代码？
3. 写入 `XXTT = 0x10` 是否会产生持续副作用？
4. `FanControl` 输入 1 和 2 的准确语义是什么？
5. 是否存在完整、可靠、可验证的恢复到原厂自动控制的步骤？
6. 它与 Fn 快捷键、性能模式和机械革命控制中心是否会互相覆盖？
7. 休眠、重启、断电后相关状态是否保留？

## 未来可接受的测试条件

只有在以下条件具备后才考虑继续：

- 使用非主力、可承受异常的兼容测试机，或由设备所有者明确接受风险。
- 已准备 BIOS/EC 恢复资料，并确认机器能够完全断电复位。
- 全程同时记录 `GFNS` 实际 RPM、温度、性能模式和返回状态。
- 第一步只研究 `GetFanSpeed`，不调用 `FanControl`。
- 在静态分析原厂服务或获得第二个独立证据前，不尝试输入值 1/2。
- 测试时有人值守，设置温度停止条件，并准备立即关机和断电。
- 不把未经验证的接口做成开机自启、后台服务或自动风扇控制。

## 当前结论

`EmdAcpi_FanControl` 对未来研究有价值，但目前不能安全地当作纯读取接口使用：

- `GetFanSpeed`：名字是读取，实际包含一次 EC 选择器写入，暂不调用。
- `FanControl`：明确写 EC，含义未确认，禁止调用。
- `GFNS`：直接读取真实测速字段，已经验证，继续作为当前唯一 RPM 来源。

截至本文记录时，只进行了 WMI 元数据查询和 ACPI 离线分析，没有调用 `GetFanSpeed` 或 `FanControl`，也没有改变风扇状态。
