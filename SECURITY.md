# 安全说明

## 项目边界

本项目当前版本只用于监控，不用于控制风扇或修改性能模式。

允许的固件操作只有固定的 `GFNS`、`GPFM` 读取请求和 WMI 事件订阅。任何加入 `FanControl`、`SPFM`、直接 EC 写入或第三方内核驱动的修改，都属于安全边界变化，必须单独审核和明确标注。

## 权限说明

目标机器只允许 `SYSTEM` 读取厂商 WMI 方法。因此启动器会在用户确认管理员授权后创建固定名称的临时 SYSTEM 计划任务。工作代码在注册任务时嵌入任务操作，不会让 SYSTEM 任务随后从用户可写路径重新加载脚本。

正常关闭窗口会删除任务。异常退出时，工作进程会在心跳超时后停止并尝试删除自身任务；另有十二小时最大运行时间。

## 报告问题

公开报告中请勿附带：

- `MSDM.bin` 或 Windows 产品密钥。
- 完整系统日志和用户名路径。
- 原厂控制中心账号、令牌或 MQTT 凭据。
- 未经确认可公开的 BIOS 或驱动二进制文件。

可以提供经过脱敏的机型、主板型号、BIOS 版本、程序日志和返回状态码。
